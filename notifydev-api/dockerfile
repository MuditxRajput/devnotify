FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 8000

# Default container process: API server.
# Worker container should override command in docker-compose, e.g.:
# command: ["node", "src/worker/worker.js"]
CMD ["node", "src/index.js"]